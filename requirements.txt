# 推理服务器项目依赖包 - 通用版本
# 同时支持CPU和GPU，运行时自动选择

# 深度学习框架 - 优先GPU，CPU降级
# 安装顺序很重要：先尝试GPU版本，失败后安装CPU版本
mindspore-gpu>=2.5.0; sys_platform != "darwin"
mindspore>=2.5.0

# YOLO模型框架
mindyolo>=0.5.0

# 计算机视觉和数值计算
opencv-python>=4.8.0
numpy>=1.21.0,<1.25.0  # 固定版本以确保兼容性

# 配置文件处理
PyYAML>=6.0

# 必需依赖
tqdm>=4.60.0
matplotlib>=3.5.0
Pillow>=8.0.0

# 性能优化
psutil>=5.8.0

# Web服务（可选）
fastapi>=0.68.0
uvicorn[standard]>=0.15.0

# 开发和调试
ipython>=7.25.0

# 额外的GPU支持库（可选）
pynvml>=11.0.0  # NVIDIA管理库
